import { AddClassOnDirective } from './toggle-class-on-click.directive';

describe('ToggleClassOnDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleClassOnDirective();
    expect(directive).toBeTruthy();
  });
});
