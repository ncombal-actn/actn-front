import { AddClassOnChangeDirective } from './add-class-on-change.directive';

describe('AddClassOnChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new AddClassOnChangeDirective();
    expect(directive).toBeTruthy();
  });
});
