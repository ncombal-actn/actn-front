import { KeyboardFocusDirective } from './keyboard-focus.directive';

describe('KeyboardFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyboardFocusDirective();
    expect(directive).toBeTruthy();
  });
});
